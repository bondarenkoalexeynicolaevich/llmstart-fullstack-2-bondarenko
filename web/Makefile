# Локальные цели Next.js (удобно из каталога web/).
# Backend, БД и общий Makefile — в корне репозитория.
.PHONY: lint web-lint dev build install start
lint web-lint:
	npm run lint

dev:
	npm run dev

build:
	npm run build

install:
	npm install

start:
	npm run start
