# Задача 04: Документация и smoke

## Цель

README: порядок `migrate` → `run-backend` → переменные бота → `run`; упоминание необходимости строк в БД (`Flow`, `User`, `Participant`). Smoke-проверка контракта API и E2E-инструкция.

## Состав работ

- Обновить «Быстрый старт» для бота: без OpenRouter в боте; `FLOW_ID`, выравнивание токена с backend.
- Цель `make` или скрипт для проверки `POST /v1/dialog-messages` (при заданных env) — см. Makefile / `scripts/`.
- `summary.md` итерации; `summary.md` по задачам 01–04; строка итерации 7 в `tasklist-backend.md` → ✅.

## DoD

- Новый разработчик видит, как засидить/вставить участника для успешного диалога.
- Статус итерации 7 обновлён в tasklist.
