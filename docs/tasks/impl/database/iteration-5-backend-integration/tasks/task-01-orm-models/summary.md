# Task 01 — summary

- Добавлены ORM: `Module`, `Lesson`, `Material`, `KnowledgeItem`, enum `MaterialType`.
- `Assignment` переведён на `lesson_id`; `Flow` — связи `modules`, `knowledge_items`.
- `User.email` — опциональный, без default `""`.
