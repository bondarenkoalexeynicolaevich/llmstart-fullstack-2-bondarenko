# Итерация 9 — summary

**Статус:** завершена.

## Результат

- Реализованы ADR-005, OpenAPI/`api-contracts`, backend `data-query`, UI режима данных в чате, тесты `backend/tests/test_data_query.py`.
- Smoke (ручной): войти учителем (`TeacherWeb`), открыть чат, включить режим данных (иконка графика), задать вопрос; при настроенном OpenRouter классификатор выберет intent по смыслу, иначе в тестах используется мок JWT+LLM.

## Отклонения от первоначального плана

- Код ошибки клиента для «не поняли вопрос»: `unrecognized_intent` (snake_case, как прочие коды backend).
- Интент `questions_count_by_lesson` считает **сдачи** по паре модуль/урок (зафиксировано в ADR-005).
