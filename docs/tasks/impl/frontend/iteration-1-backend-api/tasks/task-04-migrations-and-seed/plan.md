# Задача 04: Миграции Alembic и seed для демо UI

## Цель

- Схема БД соответствует решениям из [task-01](../task-01-data-gap-analysis/plan.md) (например `telegram_username`, индексы).
- На чистой БД после `migrate-upgrade` можно наполнить **демо-данные** для экранов 3–6 (объёмы близкие к реальным).
- Создать пользователя-преподавателя с **`telegram_id = 459032551`**; в комментарии к миграции или seed указать `@Bondarenko_Alexey_Nikolaevich` (без секретов).

## Миграции

- Одна или несколько ревизий Alembic: только необходимые DDL-изменения.
- Идемпотентность **не** требуется для raw DDL; для данных — через отдельный seed.

## Seed / demo data

Предпочтительно: скрипт Python или SQL, вызываемый из Makefile (`db-seed-frontend-demo` или расширение существующей цели):

- Один `Flow` с модулями, уроками, заданиями.
- Несколько студентов + преподаватель в `participants`.
- Смесь `Submission` со статусами и датами.
- Пакет `DialogMessage` под ленту вопросов и графики.
- При необходимости `Material` для проверки `materials` в ленте сдач.

**Username для веб-сессии:** задать `telegram_username` у демо-пользователей или у преподавателя согласно сценарию входа из итерации 0.

## Порядок

1. После финализации DDL из task-01 — написать миграцию(и).
2. Прогнать upgrade на пустую БД.
3. Прогнать seed; проверить руками ключевые SQL SELECT или через pytest/fixture.

## Документирование

- В [`README.md`](../../../../../../../README.md) или корневом Makefile help — одна строка «как поднять демо».
- В итоговом iteration `summary.md` — фиксированный `flow_id` демо (если стабильный UUID в seed).
