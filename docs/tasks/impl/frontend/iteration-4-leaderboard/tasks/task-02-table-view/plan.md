# task-02: режим таблицы

- Компонент [`web/components/leaderboard-client.tsx`](../../../../../web/components/leaderboard-client.tsx): вкладки «Таблица / Карта» (переключатель на кнопках или tabs).
- Колонки: ранг, имя, progress bar (`overall_progress`), ряд иконок по `lesson_statuses` (done / partial / none), медаль для топ-3.
- Состояния: skeleton, empty, error + retry по образцу дашборда.
