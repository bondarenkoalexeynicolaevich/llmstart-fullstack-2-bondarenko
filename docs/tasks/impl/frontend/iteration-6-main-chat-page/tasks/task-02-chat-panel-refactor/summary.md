# task-02: Рефактор ChatPanel — summary

**Статус:** Done

## Результат

- [`web/components/chat-panel.tsx`](../../../../../../web/components/chat-panel.tsx) переведён на `useChatStore()`; пропсы удалены.
- Скролл к последнему сообщению остаётся в панели через локальный `ref`.

## Отклонения от плана

- Нет.
