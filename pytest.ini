[pytest]
testpaths = backend/tests
addopts = --strict-markers
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
markers =
    integration: тесты с реальной PostgreSQL и миграциями (см. backend/tests/conftest.py)
filterwarnings =
    ignore::DeprecationWarning
